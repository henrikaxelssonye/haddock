# Agent Workflow Notes

- Do not refresh or restart the browser session after code changes unless explicitly requested by the user.
- Reuse the current Playwright/browser tab and state so manual setup/selection steps are preserved across iterations.
